
import { displayText } from 'questions/helpers/display-text';
import { module, test } from 'qunit';

module('Unit | Helper | display text');

// Replace this with your real tests.
test('it works', function(assert) {
  let result = displayText([42]);
  assert.ok(result);
});

