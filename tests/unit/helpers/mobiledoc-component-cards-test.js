
import { mobiledocComponentCards } from 'questions/helpers/mobiledoc-component-cards';
import { module, test } from 'qunit';

module('Unit | Helper | mobiledoc component cards');

// Replace this with your real tests.
test('it works', function(assert) {
  let result = mobiledocComponentCards([42]);
  assert.ok(result);
});

