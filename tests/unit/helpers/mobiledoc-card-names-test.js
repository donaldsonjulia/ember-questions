
import { mobiledocCardNames } from 'questions/helpers/mobiledoc-card-names';
import { module, test } from 'qunit';

module('Unit | Helper | mobiledoc card names');

// Replace this with your real tests.
test('it works', function(assert) {
  let result = mobiledocCardNames([42]);
  assert.ok(result);
});

