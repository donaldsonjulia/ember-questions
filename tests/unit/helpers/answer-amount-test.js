
import { answerAmount } from 'questions/helpers/answer-amount';
import { module, test } from 'qunit';

module('Unit | Helper | answer amount');

// Replace this with your real tests.
test('it works', function(assert) {
  let result = answerAmount([42]);
  assert.ok(result);
});

