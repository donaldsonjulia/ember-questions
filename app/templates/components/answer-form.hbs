<form {{action "postAnswer" on="submit"}}>
  <fieldset>
    <legend>POST AN ANSWER:</legend>
    {{input type="text" placeholder="Your name" value=author data-test-author-input=true}}
    {{simditor-editor value=textObj update=(action (mut textObj.content)) editor=(mut editor) data-test-text-input=true}}
  </fieldset>
  <button type="submit" disabled={{formIncomplete}} data-test-answer-submit>SUBMIT</button>
</form>
